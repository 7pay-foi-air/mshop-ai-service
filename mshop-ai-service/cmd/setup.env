# ai-service .env.example
PORT=8083

# JWT
JWT_SECRET=supersecretjwtkey

# Ollama (if Ollama runs on Windows host and ai-service runs in Docker, use host.docker.internal)
OLLAMA_URL=http://host.docker.internal:11434
OLLAMA_MODEL=mshop

# For local dev to disable auth:
DISABLE_AUTH=true
